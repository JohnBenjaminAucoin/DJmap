- change chart title to be playlist name
- add checkboxes to allow filtering different mix types

- work on styling (bootstrap)
- find a better way to handle multiple playlist requests (currently the next chart is draw below the first one)

- keep tabs on the development of tidal's api to add support for tidal playlists


-make sure we have the auth token before the user can press the button